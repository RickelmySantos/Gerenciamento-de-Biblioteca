{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "[infra] Reset Infrastructure",
            "type": "process",
            "command": "${workspaceFolder}/.tools/project/infra-local.sh",
            "args": ["reset"],
            "problemMatcher": []
        }
    ]
}
